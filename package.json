{
  "name": "@zhzwz/normalize.css",
  "version": "0.1.0",
  "style": "normalize.css",
  "files": [
    "normalize.css",
    "normalize.min.css"
  ],
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm run clean && pnpm install && esbuild --bundle normalize.css --minify --outfile=normalize.min.css",
    "clean": "rimraf node_modules normalize.min.css"
  },
  "devDependencies": {
    "esbuild": "^0.17.12",
    "rimraf": "^4.4.0"
  },
  "publishConfig": {
    "access": "public"
  }
}